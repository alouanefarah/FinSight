# ============================================================================
# FinSight Banking RAG System - Requirements
# ============================================================================
# Project: FinSight - RAG-Powered Banking Document Intelligence
# Author: FinSight AI Team
# Date: November 2025
# Python Version: 3.8+
# ============================================================================

# ----------------------------------------------------------------------------
# CORE DATA PROCESSING
# ----------------------------------------------------------------------------
pandas>=2.0.0,<3.0.0              # Data manipulation and DataFrame operations
numpy>=1.26.0,<2.1.0              # Numerical computing (NumPy 2.0 compatible)
requests>=2.31.0,<3.0.0           # HTTP library for Ollama API calls

# ----------------------------------------------------------------------------
# VECTOR DATABASE & EMBEDDINGS
# ----------------------------------------------------------------------------
chromadb>=0.4.24,<0.5.0           # Vector database for semantic search
sentence-transformers>=2.2.2,<3.0.0  # BGE embeddings and SentenceTransformer
torch>=2.0.0,<3.0.0               # PyTorch backend for neural networks
safetensors>=0.4.0                # Safe tensor serialization

# ----------------------------------------------------------------------------
# NLP & TRANSFORMERS
# ----------------------------------------------------------------------------
transformers>=4.30.0,<5.0.0       # Hugging Face transformers (tokenizers)
tokenizers>=0.13.3                # Fast tokenizers for text processing
huggingface-hub>=0.16.0           # Model downloading and caching

# ----------------------------------------------------------------------------
# UTILITIES & PROGRESS
# ----------------------------------------------------------------------------
tqdm>=4.66.0,<5.0.0               # Progress bars for batch processing
rich>=13.0.0,<14.0.0              # Rich text formatting for chat interface
click>=8.1.0                      # CLI creation (optional)

# ----------------------------------------------------------------------------
# DATA SERIALIZATION
# ----------------------------------------------------------------------------
pyarrow>=14.0.0                   # Parquet file support for embeddings

# ----------------------------------------------------------------------------
# OPTIONAL: GPU ACCELERATION
# ----------------------------------------------------------------------------
# For CUDA 11.8+ (uncomment if you have NVIDIA GPU):
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118

# For CUDA 12.1+ (uncomment if you have newer GPU):
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121

# For CPU-only (default, works everywhere):
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# ----------------------------------------------------------------------------
# OPTIONAL: DOCUMENT PARSING (for PDF processing)
# ----------------------------------------------------------------------------
PyPDF2>=3.0.1                     # PDF text extraction (optional)
pdfplumber>=0.10.0                # Advanced PDF parsing (optional)

# ----------------------------------------------------------------------------
# OPTIONAL: API & WEB INTERFACE
# ----------------------------------------------------------------------------
fastapi>=0.110.0                  # REST API framework (optional)
uvicorn>=0.29.0                   # ASGI server for FastAPI (optional)
python-multipart>=0.0.6           # File upload support (optional)

# ----------------------------------------------------------------------------
# OPTIONAL: DASHBOARD & VISUALIZATION
# ----------------------------------------------------------------------------
streamlit>=1.36.0                 # Interactive dashboard (optional)
matplotlib>=3.8.0                 # Plotting and visualization (optional)
plotly>=5.18.0                    # Interactive charts (optional)

# ----------------------------------------------------------------------------
# DEVELOPMENT & TESTING
# ----------------------------------------------------------------------------
python-dotenv>=1.0.0              # Environment variable management
pytest>=7.4.0                     # Testing framework (dev)
black>=23.0.0                     # Code formatting (dev)
flake8>=6.1.0                     # Linting (dev)

# ----------------------------------------------------------------------------
# COMPATIBILITY NOTES
# ----------------------------------------------------------------------------
# - NumPy 2.0+ compatible: All code uses np.ptp() instead of array.ptp()
# - Tested on Python 3.8, 3.9, 3.10, 3.11, 3.12
# - Windows, Linux, macOS compatible
# - GPU support optional but recommended for faster embeddings
#
# INSTALLATION:
# pip install -r requirements.txt
#
# MINIMAL INSTALL (core RAG only):
# pip install pandas numpy requests chromadb sentence-transformers torch tqdm rich pyarrow transformers
# ============================================================================
